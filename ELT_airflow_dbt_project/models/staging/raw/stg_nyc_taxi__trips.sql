WITH source_table AS ( 

    SELECT *
    FROM {{ source('raw', 'trips') }}

),

renaming_cols AS (

),

adding_primary_key AS (

),

vendor_name AS (

),

cleaning_datetime_cols AS (

),

passenger_count_zeros AS (

),

trip_distance_flag AS (

),

rate_code_name AS (

),

store_and_fwd_name AS (

),

payment_type_name AS (

),

